[project]
name = "agentic-autorag"
version = "0.1.0"
description = "Reasoning-driven agentic framework for RAG optimization"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    { name = "Lasse Bærland Strand" },
]
dependencies = [
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "pymupdf4llm>=0.0.17",
    "docling>=2.0",
    "langchain-text-splitters",
    "lancedb",
    "litellm",
    "numpy",
    "scipy",
    "scikit-learn",
    "sentence-transformers",
    "tqdm",
    "typer>=0.9",
    "google-cloud-aiplatform>=1.40",
    "google-auth>=2.0",
    "boto3>=1.28",
    "azure-identity>=1.15",
    "lightrag-hku>=1.4.10",
    "mteb>=2.10.9",
    "python-dotenv>=1.2.1",
    "datasets",
    "opencv-python-headless>=4.13.0.92",
]

[project.scripts]
agentic-autorag = "agentic_autorag.cli:app"

[project.optional-dependencies]
# Single dev extra bundles tooling, vLLM (GPU-only), and the baseline
# optimizer dependencies. AutoRAG is intentionally NOT listed: AutoRAG 0.3.x
# pins numpy<2, which conflicts with opencv-python-headless>=4.13. Instead,
# the autorag_* baselines shell out to AutoRAG installed in a separate venv
# pointed at by AUTORAG_PYTHON (see README).
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "ruff>=0.9",
    "arxiv>=2.0",
    "matplotlib>=3.10.8",
    "jupyterlab>=4.5.4",
    "vllm>=0.19",
    "optuna>=3.6,<5.0",
    "pyarrow>=15",
    "pandas>=2",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = [
    "slow: marks tests that are slow (e.g. PDF parsing with model downloads). Skipped by default, run with --run-slow.",
]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "SIM"]
extend-ignore = ["D102", "D103"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff.lint.isort]
known-first-party = ["agentic_autorag"]
