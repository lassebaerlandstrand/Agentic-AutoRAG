You are a RAG systems expert. Based on the corpus description and
available search space, propose an initial RAG configuration.

This is trial 1. Pick a strong starting config aimed at score — the
optimizer narrates its own stance afterward and will cost-cut once a
working frontier exists. Don't pre-optimize for cost here.

## Corpus Description
{corpus_description}

## Search Space and Expected Output Format
{search_space}

{knowledge_base}

## Task
Propose a sensible starting configuration. Consider:
1. The domain and content type described (academic papers, legal docs,
   code, etc.) — this affects chunking strategy and size.
2. The likely information density — dense technical content benefits
   from smaller chunks and higher top_k.
{graph_guidance}3. Start with a moderate, general-purpose configuration rather than
   an extreme one. The optimization loop diagnoses retrieval / ranking /
   generation bottlenecks each trial and proposes refined configs — it
   needs a reasonable baseline to diagnose from, not an ambitious one
   tuned to hunches.
4. Use the Knowledge Base to pick the best starting models: prefer the
   embedding model with the highest Retrieval score and the LLM with the
   best quality-to-cost ratio for the corpus type.
5. Start with reasoning: false unless the corpus clearly requires deep
   multi-step reasoning (e.g. math, logic, complex inference). You can
   enable reasoning in later trials if reasoning_error is the dominant
   failure pattern.

## Output
First explain your reasoning briefly, then output a single ```yaml
block matching the structure shown in the expected output format above.
Emit every TUNABLE field listed in the search space; pinned (fixed)
fields are auto-filled at parse time — do NOT include them. Do NOT nest
runtime parameters under sub-keys like 'retrieval' or 'generation' —
use the flat structure shown in the example.
