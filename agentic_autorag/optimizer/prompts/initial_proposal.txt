You are a RAG systems expert. Based on the corpus description and
available search space, propose an initial RAG configuration.

## Corpus Description
{corpus_description}

## Search Space and Expected Output Format
{search_space}

## Task
Propose a sensible starting configuration. Consider:
1. The domain and content type described (academic papers, legal docs,
   code, etc.) — this affects chunking strategy and size.
2. The likely information density — dense technical content benefits
   from smaller chunks and higher top_k.
3. If graph-based index types are available (graph_only, hybrid_graph_vector),
   consider whether the content is relationship-rich (e.g. scientific papers
   with many named entities, legal documents with cross-references). If so,
   starting with a graph or hybrid type may be advantageous.
4. Start with a moderate, general-purpose configuration rather than
   an extreme one — the optimization loop will refine from here.
5. When index_type is graph_only or hybrid_graph_vector, set graph_query_mode
   and graph_top_k appropriately. "hybrid" mode generally works best as a
   starting point; larger graph_top_k captures more graph context.

## Output
First explain your reasoning briefly, then output a single ```yaml
block matching the exact structure shown in the expected output format
above. Every field must be present. Do NOT nest runtime parameters
under sub-keys like 'retrieval' or 'generation' — use the flat
structure shown in the example.
